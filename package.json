{
    "name": "meme-coin-deployment",
    "version": "1.0.0",
    "scripts": {
        "deploy": "hardhat run scripts/deploy.js --network rinkeby"
    },
    "devDependencies": {
        "@nomiclabs/hardhat-waffle": "^2.0.0",
        "dotenv": "^10.0.0",
        "ethers": "^5.0.0",
        "hardhat": "^2.22.15"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.1.0"
    }
}
